<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>找出数组元素中的最大值</title>
</head>
<body>
    <script>
        var arr = [2, 3, 1, 12, 5, 24 ];
        var max = 0;
        for(var i = 0; i < arr.length; i++) {
            if(max < arr[i]) {
                max = arr[i];
            }
        }
        console.log(max);
    </script>
</body>
</html>
